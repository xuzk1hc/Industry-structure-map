interface:
  display_name: "Industry Map"
  short_description: "Build Markdown and HTML industry maps"
  brand_color: "#2563EB"
  default_prompt: "Use $industry-map to build an HBM industry atlas, then deliver both the reusable Markdown source and a rendered HTML consulting infographic. Use a natural-language style brief, retain creative freedom, and inspect the HTML before delivery."

policy:
  allow_implicit_invocation: true
